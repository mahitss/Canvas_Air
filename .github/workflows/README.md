# GitHub Actions Workflows Directory

This directory is designated for CI/CD workflow files:
- `build-test.yml`: Runs on PRs to master to execute build, lint, and test checks.
- `deploy.yml`: Deploys target containers on master merge.
